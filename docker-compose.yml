twitter:
  build: ./twitter
  volumes:
    - "./twitter:/src/twitter"
  ports:
    - "3000:3000"
  command: "nodemon -L twitter/app"

client:
  build: ./client
  volumes:
    - "./client:/src/client"
  ports:
    - "80:3000"
  command: "nodemon -L client/bin/www"